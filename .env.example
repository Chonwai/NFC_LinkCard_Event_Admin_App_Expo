# ─────────────────────────────────────────────────────────────────────────────
# LinkCard Event Admin App — 環境變數範本
#
# 用法：cp .env.example .env.local  然後填入值，再重啟 Metro。
#   （.env.local 已被 .gitignore 排除，不會進版控；本範本檔會進版控。）
# ─────────────────────────────────────────────────────────────────────────────
#
# EXPO_PUBLIC_API_URL = 後端 API 的 origin
#   - 只填 origin，**不含** `/api` 或任何路徑後綴
#     （service 路徑本身已是 `/api/auth/login`、`/api/v1/events/...`；
#      若這裡又填 `/api`，實際請求會變成 `/api/api/...` → 全部 404）
#   - staging 例：https://staging-api.link-card.xyz
#   - production 例：https://linkcard.xyz
#   - 本機後端例：http://127.0.0.1:3020
#   - 連本機後端時**不要用 localhost**：真機／模擬器上的 localhost 是裝置自己，
#     請改用你的 LAN IP（例：http://192.168.1.23:3020）
#
# ⚠️ 非 dev build（EAS preview / production）若缺少此值，App 會在 module-load 階段
#    throw → 白屏（無任何錯誤畫面）。見 `src/constants/config.ts` 的 fail-closed guard。
#
# EXPO_PUBLIC_WEB_URL = 用戶端網頁 origin（NFC 寫卡時寫入的 URI 前綴 `/u/:registrationId`）
#   - 只填 origin，**不含** `/u` 或任何路徑後綴
#   - 未設定時預設 `https://linkcard.xyz`
#   - 在 staging 測試寫卡時務必設定，否則寫入的卡片會指向 production
#
# ⚠️ EXPO_PUBLIC_* 是 build-time inline：改完之後**必須重啟 Metro**
#    （Ctrl+C 後重新 `npm run web` / `npm run ios`），hot reload 不會生效。
#
# ⚠️ EXPO_PUBLIC_* 會被 inline 進 bundle 並派發給任何人 → **不是放 secret 的地方**。
#    本檔刻意留空；實際 staging / prod 值請向 PM 索取，**不要寫進這個範本檔**。
#
EXPO_PUBLIC_API_URL=
EXPO_PUBLIC_WEB_URL=
